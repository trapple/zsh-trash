# Trash

ゴミ箱に入れてくれる

# INSTALL


if [-n $fpath]; then

```
echo 'fpath=($HOME/.zsh/func $fpath)' >> ~/.zshrc
```
fi

```
cp Trash $YOUR_FPATH
echo 'alias rm="trash"' >> ~/.zshrc
echo 'autoload -Uz trash; >> ~/.zshrc
```

